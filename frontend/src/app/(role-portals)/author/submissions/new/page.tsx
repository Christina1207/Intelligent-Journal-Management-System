import { NewSubmissionForm } from "@/features/submissions/components/new-submission-form";

export const metadata = {
  title: "Submit Manuscript",
};

export default function NewSubmissionPage() {
  return <NewSubmissionForm />;
}
