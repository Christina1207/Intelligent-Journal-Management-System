import { LoadingState } from "@/components/common/loading-state";

export default function ManagerLoading() {
  return (
    <LoadingState
      label="Loading Section Manager workspace"
      className="min-h-[50vh]"
    />
  );
}
